from scipy import ndimage

def meanFilter(image, mask):
    """
    takes in 2D array of pixel greymap and mask with weightings. convolves the mask over pixel array and returns
    the filtered image as 2D array.
    :param image:
    :param mask:
    :return:
    """



    raise NotImplementedError

def medianFilter():
    raise NotImplementedError